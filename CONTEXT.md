# WB-Data 领域术语

本文档只定义跨模块使用的业务术语。实现方案、接口和存储结构不放在这里。

用户用语和引擎用语不是同一套词。界面只使用本文件中的中文；代码与 Kestra 中的 Flow、Task 不进入界面。

## 项目与离线开发

**项目组（Project Group）**

隔离成员、数据源、离线开发资产和权限的协作边界。

_避免使用_：“租户”（当前系统尚未定义完整的多租户语义）。

**任务（Flow）**

用户创建、保存、发布并执行的离线处理单元，由多个节点和依赖组成。任务有身份（仓库中的路径）和一份当前任务文档。

_避免使用_：流程、Flow（仅代码与 Kestra 使用）；也不要用任务指单个节点或一次执行。

**任务文档（Flow Document）**

一份任务当前可编辑的内容：节点、依赖、布局、调度和参数绑定。本地仓库里每个任务只有一份当前文档。

_避免使用_：用 document 指 YAML、Git 文件或浏览器里未保存的草稿。

**草稿（Draft）**

尚未保存的任务文档工作副本。保存之前它不属于本地仓库。

_避免使用_：editingSession；也不要把已保存但未提交的改动叫作草稿。

**恢复稿（Recovery Draft）**

本机留下的未保存草稿副本。它仍是草稿，不是仓库中的任务文档。

**节点（Node）**

任务中的一个执行单元，例如 SQL、Hive SQL、Shell 或数据传输节点。引擎里对应 Kestra Task；用户侧只叫节点。

_避免使用_：任务、流程任务、task。

**执行（Execution）**

某个任务在特定输入和时间上下文下的一次运行实例。调试执行和运维执行都是执行。

_避免使用_：用任务指一次运行。失败时说「任务执行失败」或「节点失败」，不要只说「任务失败了」。

**调试执行（Debug Execution）**

针对当前草稿或已保存任务文档发起的执行。它不发布任务，也不会出现在运维中心。

_避免使用_：“测试”或“试跑”作为正式名称。

**运维执行（Operations Execution）**

已发布任务的一次运行，包括调度触发和运维重跑。运维中心只列出这类执行。

_避免使用_：“生产任务”；也不要把调试执行算进运维。

## 仓库与发布

**工作区（Workspace）**

用户登录后、在当前项目组内操作的产品界面。

_避免使用_：用“工作区”指项目组、本地仓库或 Git 工作树。

**本地仓库（Local Repository）**

项目组用于离线开发的 Git 仓库副本，包含工作树和提交历史。

_避免使用_：repo 当作用户用语；也不要用“工作区”代替本地仓库。

**工作树（Working Tree）**

本地仓库当前检出到磁盘上的文件。保存写入工作树；提交才把它记入仓库历史。

_避免使用_：用“工作区”指未提交改动。

**远程仓库（Remote Repository）**

用于成员同步、并把本地提交发布出去的 Git 远端。

**提交（Commit）**

本地仓库历史中的一次已记录快照。已保存但未提交的改动仍只存在于工作树。

_避免使用_：把保存或发布叫作提交。

**发布（Publish）**

把已提交的任务提供给调度和运维执行。保存和调试执行都不是发布。

## 参数

**参数组（Parameter Group）**

项目组内可复用的一组参数定义。一个任务可以绑定多个参数组，绑定后的定义按键合并进同一份参数快照。

**参数定义（Parameter Definition）**

参数的稳定键、取值来源和来源配置；它描述如何得到字符串值，不代表某次执行的最终值。

**参数键（Parameter Key）**

在参数组内唯一且区分大小写的参数标识，例如 `v_day`。

**取值来源（Value Source）**

参数值的产生方式。V1 只有固定值和时间两种来源。

**常量参数（Constant Parameter）**

值在参数定义中明确给出，并在参数组版本不变时保持不变的参数。

**时间参数（Time Parameter）**

根据本次执行的时间上下文、任务运行时区、偏移量和格式规则计算字符串值的参数。

_避免使用_：“动态参数”或“运行时参数”（范围过宽，容易被理解为任意表达式）。

**参数绑定（Parameter Binding）**

任务对一个参数组的显式引用关系。绑定不等于自动跟随参数组后续修改。

**参数组版本（Parameter Group Version）**

参数组定义内容的一次可识别版本。只有定义内容实际变化时才产生新版本，供任务文档判断绑定是否过期。

_避免使用_：用 version 表示并发控制，或把名称、说明、归档状态的修改都当成新版本。

**参数组修订（Parameter Group Revision）**

参数组的并发修订号。任何实际修改都会递增，用来避免覆盖他人的更新。它不是定义内容版本，也不是 Git 提交。

_避免使用_：把 revision 与 version 混用，或用 revision 指任务文档、草稿或 Git 提交。

**参数快照（Parameter Snapshot）**

任务保存或发布时复制的、不可变的参数组版本内容，用于保证已发布任务可重复构建和运行。

**参数覆盖（Parameter Override）**

手动执行时为已有参数键临时提供的值；覆盖只影响本次执行，不修改参数组或参数快照。

**已解析参数集（Resolved Parameter Set）**

某次执行最终使用的参数键值集合。每个值都已经过来源计算和覆盖合并，并在该次执行中保持不变。

## 时间语义

**执行时间上下文（Execution Time Context）**

一次执行用于解析时间参数的不可变上下文，由可选的计划时间、执行开始时间和任务运行时区组成。

**任务运行时区（Flow Runtime Timezone）**

解释任务调度时间和时间参数的唯一时区。它属于任务，不属于参数组或单个参数。

_避免使用_：浏览器时区、服务器时区、容器时区。

**时间基准（Time Basis）**

时间参数明确选择的参考时间，只能是计划时间或执行开始时间；两者不得自动相互回退。

**计划时间（Planned Time）**

调度器原计划触发某次任务执行的时间。延迟、节点重试或历史重跑不会改变该计划时间。

**执行开始时间（Execution Start Time）**

一次任务执行实例实际创建或开始的时间。它在整次执行中保持不变，不是节点开始时间，也不是解析参数时的当前时间。

_避免使用_：“实际运行时间”（容易与节点开始时间或当前时间混淆）。

**模拟计划时间（Simulated Planned Time）**

新建手动执行时，由用户为计划时间参数提供的计划时间。它只属于本次执行，不会创建或修改调度配置。
